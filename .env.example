# Redis Configuration
# Choose one based on your environment:

# Option 1: Local Redis (docker or local install)
REDIS_HOST=localhost
REDIS_PORT=6379

# Option 2: K3s NodePort (access from outside cluster)
# REDIS_HOST=192.168.18.40
# REDIS_PORT=30379

# Option 3: K3s Internal (from within cluster)
# REDIS_HOST=redis.extracto.svc.cluster.local
# REDIS_PORT=6379

# MongoDB Configuration
# Choose one based on your environment:

# Option 1: Local MongoDB (docker or local install)
MONGODB_URI=mongodb://admin:extracto-secure-password-2025@localhost:27017

# Option 2: K3s NodePort (access from outside cluster)
# MONGODB_URI=mongodb://admin:extracto-secure-password-2025@192.168.18.40:30017

# Option 3: K3s Internal (from within cluster)
# MONGODB_URI=mongodb://admin:extracto-secure-password-2025@mongodb.extracto.svc.cluster.local:27017

# Option 4: Port Forward (when using kubectl port-forward)
# MONGODB_URI=mongodb://admin:extracto-secure-password-2025@localhost:27017

# Database Name
MONGODB_DATABASE=extracto

# Environment
NODE_ENV=development
